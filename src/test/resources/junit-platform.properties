# Run test classes in the order they declare, so the smoke test goes first.
#
# It exists to separate "is the rig sound" from "is the feature working", which is only useful if
# it runs before the feature tests: a failure there tells you to stop reading the rest.
junit.jupiter.testclass.order.default=org.junit.jupiter.api.ClassOrderer$OrderAnnotation
